# 09. سجل التقدم والمنجزات (Progress Log)

سجل زمني لتوثيق المراحل المنجزة، التغييرات في الملفات، والمشاكل المحلولة.

---

## 📅 سجل التحديثات

### 🔹 22 أغسطس 2026 - (مراجعة شاملة أولى للمشروع + توثيق أولي)
* **الحدث:** أول مراجعة فنية كاملة للمشروع بعد اكتشافه كمشروع منفصل عن `mamelon-erp` يشارك نفس قاعدة البيانات. التوثيق حينها كُتب في ملف واحد: `REVIEW_AND_HANDOFF.md` (تجاوزه لاحقاً بمجلد `docs/` الحالي بتاريخ 25 أغسطس 2026).
* **ما تم إنجازه:**
  1. فحص كامل لدورة عمل المندوب (بحث → التقاط → سلة → تسليم/إرجاع) وتوثيقها مطابقة تماماً للكود الفعلي.
  2. تحديد 3 نقاط بدت كأخطاء محتملة عند الفحص الأول، لكن تبيّن بعد نقاش مباشر مع المستخدم أنها سلوك مقصود — وثِّقت لاحقاً كـ BR-C01, BR-C02, BR-C04 في `07_BUSINESS_RULES.md`.
  3. رصد ثغرتين أمنيتين مفتوحتين (توكن تليجرام مكشوف، وXSS مخزَّن بلا تهريب) — وثِّقتا في `06_API.md`/`11_TESTING.md`، بانتظار قرار المستخدم بشأن أولوية معالجتهما.
  4. رصد غياب تام لدعم العمل أوفلاين — معلومة للسياق فقط، لم يُطلَب أي عمل.
* **الملفات المعدّلة:** لا شيء في الكود — توثيق فقط (`REVIEW_AND_HANDOFF.md`).

---

### 🔹 25 أغسطس 2026 - (ميزة جديدة: تسليم دفعة حالات لنفس العيادة بخطوة واحدة)
* **السياق:** مندوب أبلغ المستخدم أن تسليم عدة تركيبات (4-8) لنفس العيادة كان يتطلب فتح كل حالة لحالها، اختيار نوع منتج، وكتابة اسم المستلم من جديد في كل مرة — رغم أن المستلم نفس الشخص عادة.
* **قراران واعيان أُكِّدا مجدداً قبل التنفيذ (يوازيان BR-C02/BR-C03):**
  1. دمج المواد المتعددة لنفس رقم الحالة عند التسليم سلوك مقصود — لا يُلمَس.
  2. اختيار نوع المنتج يبقى يدوياً دائماً ومستقلاً عن `cases.product_type` — فقط يتغيّر توقيته (ينتقل لمرحلة الالتقاط بدل التسليم).
* **ما تم إنجازه:**
  1. **الجزء أ:** منتقي نوع منتج جديد (اختياري) في تبويب "البحث" — يُحفَظ محلياً في `localStorage` (`mamelon_pending_products`, مفتاحه رقم الحالة) بانتظار لحظة التسليم.
  2. **الجزء ب:** تبويب "سلتي" أصبح يجمّع البطاقات بصرياً حسب العيادة (`order=clinic_name.asc,case_number.asc` + حقن عناوين عيادات)، مع صناديق اختيار متعددة مقيَّدة بعيادة واحدة فقط في كل مرة (قفل تلقائي على أول عيادة يُحدَّد منها صندوق)، وشريط سفلي مشترك لتسليم الدفعة بحقل اسم مستلم واحد.
  3. تسليم الدفعة يُنشئ صفاً منفصلاً في `deliveries` **لكل حالة** (بنوع منتجها الخاص المُختار مسبقاً)، مع تحديث حالة كل الحالات دفعة واحدة عبر `PATCH ... case_number=in.(...)`، وإشعار تليجرام واحد مجمَّع (وليس رسالة لكل حالة).
  4. المسار الفردي القديم (تسليم حالة واحدة) أُبقي بلا أي تغيير موازٍ — يعمل جنباً إلى جنب مع الدفعة، مع تعطيل تلقائي لزر التسليم الفردي لأي بطاقة محدَّدة ضمن دفعة (منع إرسال مزدوج متعارض).
  5. خط رجوع: لو حالة ضمن الدفعة بلا نوع منتج مُختار مسبقاً، يُمنَع التأكيد الجماعي مع تنبيه يسمّي الحالة الناقصة تحديداً.
* **🐞 عطل اكتُشف وأُصلح أثناء الاختبار الفعلي (لم يكن جزءاً من الخطة الأصلية):** زر "تأكيد تسليم (N)" كان يحتوي عنصر `<span>` منفصل لعداد الحالات المحدَّدة داخله؛ استبدال نص الزر بالكامل (`textContent = '...جاري التسجيل'`) وقت الإرسال كان يمحو هذا العنصر نهائياً من الصفحة، فيتعطّل عداد أي دفعة لاحقة بصمت (بلا أي رسالة خطأ ظاهرة). أُصلح بفصل نص الزر عن العداد في عنصر `<span id="batchConfirmBtnText">` مستقل، لا يُلمَس عند تغيير حالة التحميل.
* **التحقق — اختبار طرف لطرف كامل على قاعدة الإنتاج الفعلية (لا افتراضي):**
  - إنشاء 6 حالات اختبار حقيقية (`TEST-BATCH-01` إلى `06`) عبر SQL مباشر، أغلبها بنفس اسم عيادة وواحدة بعيادة مختلفة.
  - تسجيل دخول فعلي كمندوب اختبار ("مندوب 3") عبر خادم HTTP محلي (وليس `file://` مباشرة، لأن `localStorage` مُعطَّل على أصل `file://` وهو أساسي لعمل هذه الميزة بالكامل).
  - تسليم دفعة 3 حالات دفعة واحدة: تحقُّق SQL مباشر أن كل حالة سجَّلت **نوع منتجها الخاص** الصحيح مع **نفس اسم المستلم** المشترك، وأن الحالات الثلاث صارت `delivered`.
  - تحقُّق أن قفل العيادة يعمل: صندوق حالة بعيادة مختلفة صار `disabled` تلقائياً فور تحديد أول صندوق.
  - تحقُّق أن خط الرجوع (حالة بلا منتج مُختار) يمنع التأكيد ويسمّي الحالة الناقصة بالضبط.
  - تحقُّق أن المسار الفردي (حالة واحدة، بلا تحديد صندوق) لا يزال يعمل بلا أي كسر — تسليم حالة اختبار سادسة بالطريقة القديمة تماماً، ونجح.
  - اكتشاف عطل الزر ("...جاري التسجيل" يمحو عداد الدفعة) بعد إعادة تحميل الصفحة واختبار دفعة ثانية — تأكيد الإصلاح بإعادة الاختبار الكامل بعده.
  - حُذفت كل بيانات الاختبار الستة من قاعدة الإنتاج فوراً بعد التحقق النهائي.
* **الملفات المعدّلة:** `MANDOB GEMNI.html` فقط (CSS: `.clinic-header`/`.checkbox.disabled`؛ HTML: عنصر `deliverBatchBar` جديد؛ JS: `renderSearchedCases()`, `loadCart()`, دوال جديدة: `savePendingProduct`, `clearPendingProduct`, `applyClinicLock`, `updateBatchToolbar`, `checkBatchConfirmEnabled`, `confirmBatchDelivery`).
* **لا تعديل على قاعدة بيانات Supabase** — لا عمود جديد، لا جدول جديد، لا سياسة RLS جديدة.
* **القرار المعماري:** راجع `ADR-C01` في `docs/10_DECISIONS.md`.

---

### 🔹 25 أغسطس 2026 - (إنشاء نظام توثيق `docs/` كامل لهذا المشروع)
* **الطلب:** بناء نفس بروتوكول التوثيق المستخدَم في `mamelon-erp` (`CLAUDE.md` + `docs/` مرقَّم) لهذا المشروع، معبَّأً بمحتوى المشروع الفعلي.
* **ما تم إنجازه:** إنشاء `CLAUDE.md` + 9 ملفات `docs/` (`00`, `02`, `03`, `04`, `06`, `07`, `09`, `10`, `11`, `12`)، مبنية على محتوى `REVIEW_AND_HANDOFF.md` السابق (لم يُحذَف، يبقى للأرشيف) بالإضافة لميزة تسليم الدفعة المُنجَزة في نفس اليوم.
* **الملفات الجديدة:** `CLAUDE.md`, `docs/README.md`, `docs/00_PROJECT_CONTEXT.md`, `docs/02_ARCHITECTURE.md`, `docs/03_DATABASE.md`, `docs/04_FEATURES.md`, `docs/06_API.md`, `docs/07_BUSINESS_RULES.md`, `docs/09_PROGRESS.md` (هذا الملف)، `docs/10_DECISIONS.md`, `docs/11_TESTING.md`, `docs/12_AGENT_HANDOFF.md`.

---

### 🔹 25 أغسطس 2026 - (إغلاق الثغرتين الأمنيتين المعروفتين: توكن تليجرام مكشوف + XSS مخزَّن)
* **السياق:** المستخدم قرر رفع هذا المشروع لريبو GitHub **عام** (لخدمة الاستضافة المجانية + تطبيق APK اللي يقرأ الرابط الحي — راجع محادثة "WebIntoApp"). هذا رفع أولوية الثغرتين الموثَّقتين مسبقاً من "معروفتين وغير مستعجلتين" إلى "لازم تُحلّان قبل الرفع العلني". طلب المستخدم صراحةً تنفيذ الحلّين معاً.
* **1) حماية توكن بوت تليجرام:** التوكن نُقل بالكامل خلف دالة Supabase Edge Function جديدة (`telegram-notify`، منشورة فعلياً وتعمل). حُذفت ثوابت `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` نهائياً من `MANDOB GEMNI.html`. راجع **ADR-C03** للتفاصيل الكاملة.
  - **⚠️ خطوة متبقية على المستخدم فقط:** ضبط قيمتَي `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` كأسرار (Secrets) على الدالة عبر لوحة تحكم Supabase أو الـ CLI — لم تُطلَب القيمة الفعلية ولم تُكتَب في أي مكان بهذه الجلسة (قصداً، لتفادي التعامل مع السرّ الفعلي). قبل ضبطها، أي إشعار تليجرام سيفشل بصمت (الدالة تُرجِع خطأ `Server not configured` بدل الإرسال).
* **2) تطهير المخرجات (`escapeHtml`):** طُبِّقت على كل مواضع `innerHTML` الثلاثة (`renderSearchedCases()`, `loadCart()`, `loadReturned()`) لكل قيمة قادمة من قاعدة البيانات. راجع **ADR-C04**.
* **التحقق — كلاهما فعلي على قاعدة الإنتاج (لا افتراضي):**
  - **الدالة:** اختبار `curl` مباشر بمفتاح `anon` الحقيقي — استجابت بنجاح تقني (`verify_jwt` نجح، التوجيه صحيح)، مع خطأ متوقَّع `Server not configured` لغياب الأسرار حتى الآن.
  - **XSS:** حالة اختبار حقيقية بحمولة هجومية فعلية (`<img src=x onerror=alert(1)>`, `<script>`) في `clinic_name`/`patient_name` — عُرضت في السلة الفعلية عبر متصفح حقيقي، ظهرت كنص حرفي مرئي بلا أي تنفيذ (لا عنصر `<script>`/`<img>` حقيقي في DOM، لا تنفيذ للكود). حُذفت حالة الاختبار فوراً بعد التحقق.
* **الملفات المعدّلة:** `MANDOB GEMNI.html`. **مورد جديد على Supabase (خارج هذا المستودع):** Edge Function `telegram-notify`.
* **القرار المعماري:** راجع `ADR-C03` و`ADR-C04` في `docs/10_DECISIONS.md`.

---

### 🔹 25 أغسطس 2026 - (رفع الريبو لعام + تفعيل GitHub Pages + إعادة تسمية الملف لـ `index.html`)
* **السياق:** المستخدم دفع كل تعديلات اليوم للريبو (`ahbayo-dotcom/mamelon-lab-2026-delivery`)، ثم حوّله لعام (Public) عبر GitHub UI مباشرة (Settings → Danger Zone) لخدمة استضافة GitHub Pages المجانية + تطبيق APK يقرأ رابطاً حياً (WebIntoApp).
* **مشكلة 1 — "Site not found" بعد التحويل لعام:** الرابط المتوقَّع (`https://ahbayo-dotcom.github.io/mamelon-lab-2026-delivery/...`) رجع صفحة "Site not found" رغم أن إعدادات Pages تظهر `main`/`(root)` محدَّدة. **السبب الجذري (تأكَّد عبر `GET /repos/.../actions/runs` — `total_count: 0`):** **GitHub Pages للريبوهات الخاصة غير متاح على الحساب المجاني الشخصي** — فالصفحة لم تُبنَ أي مرة إطلاقاً طول ما كان الريبو خاصاً، بغض النظر عن الإعدادات المعروضة (زر "Save" الرمادي بجانب اختيار الفرع لم يكن عالقاً — كان طبيعياً لأنه لا يوجد تغيير جديد ليُحفَظ، والمشكلة كانت في الأهلية لا الإعداد). آخر دفعة سابقة صارت **قبل** التحويل لعام، فما فيه أي دفعة تالية تُشغِّل أول بناء بعد التأهّل.
  * **الحل:** دفعة فارغة (`git commit --allow-empty`) بعد التحويل لعام — شغّلت أول بناء Pages فعلي بنجاح (`status: completed, conclusion: success`)، تحقَّق منه مباشرة عبر API.
* **مشكلة 2 — 404 "File not found" من WebIntoApp تحديداً:** المستخدم أبلغ أن الرابط (بصيغة `MANDOB%20GEMNI.html` بمسافة مُرمَّزة) لا يعمل عند اختباره من أداة التغليف (WebIntoApp)، رغم أنه اشتغل فعلياً بفحص مباشر بالمتصفح من طرفي. **السبب المرجَّح:** أدوات تغليف مختلفة (WebIntoApp وغيرها) قد لا تتعامل بشكل موثوق مع ترميز `%20` بالمسافات داخل أسماء الملفات.
  * **الحل الدائم (بموافقة المستخدم):** إعادة تسمية `MANDOB GEMNI.html` → **`index.html`** (عبر `git mv` للحفاظ على تاريخ الملف). هذا يفعّل الرابط الجذري البسيط بلا أي مسافات أو ترميز:
    ```
    https://ahbayo-dotcom.github.io/mamelon-lab-2026-delivery/
    ```
* **التحقق:** بعد كل دفعة، انتُظر اكتمال بناء Pages فعلياً (`polling` على `actions/runs` حتى `status: completed`)، ثم فُتح الرابط الجديد مباشرة عبر متصفح حقيقي وتأكَّد: العنوان صحيح (`Mamelon Lab · تطبيق المندوب`)، ووجود `escapeHtml` كدالة فعلية في الصفحة المحمَّلة (تأكيد أنها تخدم آخر نسخة من الكود، لا نسخة قديمة مخبَّأة).
* **⚠️ ملاحظة مهمة على أي رابط قديم:** أي مكان محفوظ فيه الرابط القديم بالمسافة (`MANDOB%20GEMNI.html` أو `MANDOB GEMNI.html`) **لن يعمل بعد الآن** — استُبدل بالكامل بـ`index.html`. حدِّث أي إشارة/رابط محفوظ (بما فيها إعداد WebIntoApp) للرابط الجذري الجديد.
* **الملفات المعدّلة:** إعادة تسمية `MANDOB GEMNI.html` → `index.html` (بلا تغيير بالمحتوى)، تحديث كل الإشارات لاسم الملف في `CLAUDE.md`، `docs/02_ARCHITECTURE.md`، `docs/06_API.md`، `docs/12_AGENT_HANDOFF.md` (السجلات التاريخية في هذا الملف وفي `10_DECISIONS.md`/`REVIEW_AND_HANDOFF.md` تبقى بالاسم القديم عمداً — دقيقة لوقتها، لا تُعاد كتابة التاريخ).

---

### 🔹 26 أغسطس 2026 - (إصلاح حرج: إشعارات التليجرام تفشل بصمت من التطبيق الحقيقي — عطل CORS)
* **البلاغ:** بعد فتح التطبيق على الهاتف من الرابط الجديد المستضاف، جرّب المستخدم تسليم/إرجاع حالة حقيقية — **الحالة تحدَّثت بنجاح، لكن لا إشعار تليجرام وصل إطلاقاً.**
* **السبب الجذري:** دالة `telegram-notify` (ADR-C03) اختُبرت سابقاً عبر `curl` فقط — وهذا لا يفرض قيود CORS إطلاقاً. التطبيق الحقيقي (مستضاف على GitHub Pages) يستدعيها من متصفح فعلي بترويسات مخصَّصة، فيرسل المتصفح طلب Preflight (`OPTIONS`) أولاً — والدالة لم تكن تتعامل معه، فرفضه المتصفح بصمت. `sendTelegramNotification()` تبتلع الأخطاء الشبكية عمداً (حتى لا يُعطِّل نجاح التسليم نفسه)، فلم يظهر أي خطأ للمستخدم رغم فشل الإشعار الكامل.
* **الإصلاح:** إعادة نشر `telegram-notify` (نسخة 4) مع معالجة `OPTIONS` وترويسات `Access-Control-Allow-*` كاملة على كل استجابة. راجع **ADR-C05** للتفاصيل التقنية الكاملة.
* **التحقق:** `curl -X OPTIONS` بترويسات مطابقة للمتصفح فعلياً أرجع `204` مع ترويسات CORS صحيحة، واستدعاء حقيقي لـ`sendTelegramNotification()` من نفس صفحة `index.html` المستضافة (نفس منشأ التطبيق الحقيقي تماماً) اكتمل بلا أي خطأ كونسول. **بانتظار تأكيد نهائي من المستخدم** بوصول الإشعار فعلياً من الهاتف.
* **درس مهم موثَّق في `11_TESTING.md`:** اختبار `curl` لا يكفي أبداً للتحقق من نداءات API ستُستدعى من متصفح حقيقي بمنشأ مختلف — لازم اختبار CORS صريح (Preflight فعلي أو نداء من صفحة حقيقية بنفس المنشأ).
* **الملفات المعدّلة:** لا شيء بهذا المستودع — دالة `telegram-notify` على Supabase فقط.
* **القرار المعماري:** راجع `ADR-C05` في `docs/10_DECISIONS.md`.

---

### 🔹 27 أغسطس 2026 - (تشخيص وإصلاح: الكاميرا لا تعمل إطلاقاً داخل APK بعد آخر تحديث)
* **البلاغ:** المستخدم أبلغ أن الكاميرا (مسح QR/باركود) كانت تعمل قبل آخر تحديث، وتوقفت بعده تماماً — لا خطأ ظاهر، "لا يحدث شيء إطلاقاً" عند الضغط على زر المسح، فقط داخل الـ APK المُثبَّت على هاتف المندوب (تأكَّد لاحقاً أن الكود سليم 100% ويعمل عادي من متصفح حقيقي).
* **التشخيص:**
  1. فحص كامل لتاريخ `git log --all -p` أثبت أن كود الماسح (`startScanner`/`stopScanner`) وسطر تحميل مكتبة `html5-qrcode` لم يُلمَسا إطلاقاً منذ أول كوميت — فالسبب ليس حذف كود.
  2. المستخدم تحقَّق من إعدادات أندرويد → صلاحيات التطبيق: **خانة الكاميرا غير موجودة إطلاقاً** (فقط الإشعارات ظاهرة) — يعني `AndroidManifest.xml` بتاع الـ APK الحالي لم يُبنَ بصلاحية `android.permission.CAMERA` من الأساس، وليس أن المستخدم رفضها.
  3. المستخدم أكَّد أن أداة التغليف (WebIntoApp) **ليس بها خيار يدوي لتفعيل صلاحيات** — الأداة تكتشف الصلاحيات المطلوبة تلقائياً بالزحف على الصفحة وقت البناء. الفرضية الجذرية: تحميل `html5-qrcode` من `unpkg.com` (رابط خارجي غير مثبَّت الإصدار) قد يكون فشل أو تأخر وقت زحف الأداة، فلم تكتشف استخدام الكاميرا، فلم تُضِف الصلاحية للـ manifest عند بناء الـ APK الأخير.
* **السبب الجذري المؤكَّد:** صلاحية أندرويد الأصلية (native) تُحدَّد فقط وقت تغليف/بناء الـ APK ولا تتأثر أبداً بتحديثات محتوى الويب اللاحقة (حتى لو الصفحة أصبحت مثالية) — لازم إعادة تغليف كاملة بعد أي إصلاح.
* **الإصلاح المُنفَّذ في هذا المستودع:** استبدال تحميل `html5-qrcode` من CDN خارجي (`unpkg.com`, بلا تثبيت إصدار) بملف محلي مثبَّت الإصدار (`html5-qrcode.min.js`, v2.3.8) داخل مجلد المشروع. الهدف: ضمان أن أداة التغليف تكتشف استخدام الكاميرا بشكل مؤكد (لا اعتماد على نجاح تحميل CDN خارجي وقت الزحف)، وأيضاً تثبيت نسخة معروفة بدل "آخر إصدار" غير محدد.
* **تحقُّق محلي:** خادم HTTP محلي مؤقت (`python -m http.server`) + تصفّح حقيقي عبر Browser pane — تأكَّد أن `typeof Html5Qrcode === "function"` (تحميل ناجح من الملف المحلي) ولا أخطاء كونسول.
* **⚠️ خطوة إجبارية متبقية على المستخدم (خارج هذا المستودع):** هذا الإصلاح وحده **لا يكفي** لحل المشكلة على هاتف المندوب — الصلاحية "محفورة" في الـ APK المُثبَّت حالياً ولن تتغيّر بتحديث الرابط الحي. لازم: (1) دفع هذا التعديل لـ GitHub، (2) إعادة توليد APK جديد بالكامل من WebIntoApp بعد أن يقرأ النسخة الجديدة، (3) تثبيت الجديد على الهاتف بدل القديم، (4) التحقق من ظهور خانة صلاحية الكاميرا في إعدادات أندرويد بعد التثبيت.
* **الملفات المعدّلة:** `index.html` (سطر تحميل المكتبة فقط)، ملف جديد `html5-qrcode.min.js` (مكتبة طرف ثالث، غير مصدر داخلي).

---

### 🔹 27 أغسطس 2026 - (تعديل تصميمي: حذف الإطار الذهبي المحيط بأيقونة التطبيق)
* **الطلب:** المستخدم طلب حذف الإطار/الحلقة الذهبية المحيطة بحافة `icon.jpg` (أيقونة التطبيق) بدون المساس بأي عنصر آخر (الروبوت، النصوص، الأيقونات الداخلية، الخلفية الكحلية).
* **التنفيذ:** عبر معالجة صورة محلية (Python/Pillow، لا أدوات خارجية) — تحديد نصف قطر الحلقة الذهبية بالتحقق من عدة زوايا حول محيط الدائرة (تراوح تقريبًا بين نصف قطر 489-510 بكسل حول مركز الصورة)، ثم طلاء الشريط الدائري (نصف قطر 483-516 بكسل، بهامش أمان) بالأسود ليطابق الخلفية الخارجية للأيقونة — لا تغيير على أي بكسل خارج هذا الشريط.
* **جولة تعديل مرفوضة:** جُرِّب أيضًا حذف كل النصوص من الأيقونة (MAMELON/DENTAL LAB/DELIVERY إلخ عبر inpainting بـ OpenCV) بناءً على طلب مبدئي من المستخدم، لكن بعد المعاينة **رفض المستخدم هذه النسخة** وطلب الرجوع للنسخة السابقة (حذف الإطار فقط، مع بقاء كل النصوص). لم يُطبَّق حذف النصوص على الملف النهائي.
* **الاعتماد النهائي:** المستخدم اعتمد نسخة "حذف الإطار الذهبي فقط" — استُبدلت بها `icon.jpg` الأصلية مباشرة.
* **الملفات المعدّلة:** `icon.jpg` (استبدال مباشر، نفس الاسم — يُستخدَم كما هو في `manifest.json`، لا حاجة لتعديل هناك).
