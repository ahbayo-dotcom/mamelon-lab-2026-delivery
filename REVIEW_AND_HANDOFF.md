# مراجعة تطبيق المندوب (Courier PWA) — تقرير ومتابعة

> [!NOTE]
> **📌 هذا الملف أرشيف تاريخي.** منذ 25 أغسطس 2026، المرجع الحي المعتمد لهذا المشروع هو مجلد
> [`docs/`](./docs/README.md) (بنفس بروتوكول التوثيق المستخدَم في `mamelon-erp`) + [`CLAUDE.md`](./CLAUDE.md).
> محتوى هذا الملف نُقل بالكامل إلى الملفات المرقَّمة هناك — راجعه للأرشفة فقط، لا كمصدر حالة حالية.

> هذا الملف مخصَّص لفتح محادثة جديدة تتابع العمل على **هذا المشروع فقط**
> (`C:\000\PROJECT\mamelon-lab-2026 delivery`). **لا علاقة له بمشروع `mamelon-erp`
> ولا يجوز دمج أي تعديل هنا في ذلك المشروع، ولا العكس** — هذا قرار صريح من المستخدم.

---

## 1. السياق والبنية

- **الملفات:** `MANDOB GEMNI.html` (1280 سطراً، HTML+CSS+JS في ملف واحد، بلا أدوات بناء ولا React) + `manifest.json` + `icon.jpg`.
- **الطبيعة:** تطبيق PWA (Progressive Web App) يعمل من هاتف المندوب مباشرة عبر المتصفح، بلا حزم ولا تثبيت تقليدي.
- **قاعدة البيانات:** يتصل بـ **نفس مشروع Supabase** الذي يستخدمه `mamelon-erp` حرفياً (نفس المعرّف `qhhqodxbhhvhkjtzmmsh`) — تأكَّد ذلك بالمقارنة المباشرة. أي تغيير في جداول `cases`/`deliveries`/`returns`/`delegate_directory` من أي الطرفين ينعكس فوراً على الآخر (لا مزامنة يدوية، اتصال مباشر لنفس القاعدة).
- **الجداول المستخدمة:**
  - `cases` (قراءة/تعديل `status`, `current_holder`, `picked_up_at`)
  - `deliveries` (إدراج عند التسليم)
  - `returns` (إدراج عند الإرجاع)
  - `delegate_directory` (view للمصادقة برمز PIN — قراءة فقط)
- **المصادقة:** رمز PIN من 4 أرقام يُطابَق مباشرة مع `delegate_directory`، محفوظ في `localStorage` باسم `mamelon_delegate_user` — نفس نمط الحماية على مستوى الواجهة المستخدم في `mamelon-erp` (لا Supabase Auth حقيقي).
- **الباركود:** مكتبة `html5-qrcode` (CDN عبر `unpkg.com`) لمسح كاميرا الهاتف.
- **إشعارات:** بوت تليجرام يرسل رسالة عند كل تسليم/إرجاع.

### دورة عمل المندوب (مؤكَّدة من الكود مباشرة)
1. **تبويب البحث:** يكتب/يمسح رقم الحالة → يظهر في قائمة مؤقتة (لا تغيير في القاعدة بعد).
2. **زر «📦 خد المحدد للسلة»** (`confirmPickup()`): هنا فقط يحدث `PATCH` فعلي:
   ```js
   { status: 'out_for_delivery', current_holder: currentUser.name, picked_up_at: new Date().toISOString() }
   ```
   يُطبَّق على **كل** صفوف `case_number` (بشرط `current_holder=is.null`، لمنع تعارض مندوبَين) — أي الحالة تتحول لـ«قيد التوصيل» ويُسجَّل حائزها فوراً عند الإضافة للسلة. **هذا يعمل بالفعل كما هو مطلوب — لا يحتاج أي تعديل.**
3. **تبويب سلتي:** يعرض حالات `out_for_delivery` الخاصة بالمندوب الحالي، مع زر تسليم أو إرجاع لكل حالة.
4. **تسليم:** يختار المندوب منتجاً واحداً من قائمة ثابتة + اسم المستلم → يُدرَج صف في `deliveries` (منتج واحد فقط) ثم `PATCH` يُحوّل **كل** صفوف `case_number` إلى `delivered` دفعة واحدة، ويُرسَل إشعار تليجرام.
5. **إرجاع:** سبب اختياري → إدراج في `returns`، وإرجاع الحالة لـ`ready` (لا `registered`).

---

## 2. الفحوصات والقرارات المتخذة في الجلسة السابقة

| البند | الحالة | القرار |
|---|---|---|
| قائمة المنتجات الخمسة الثابتة (`PMMA, ZIRCON, EMAX, Implant Zircon, Night Guard`) بينما القاعدة الفعلية فيها 13 منتجاً | ✅ **ليست خطأً** | **مقصودة عمداً** — المستخدم يريد تبسيط الاختيار على المندوب بدل إرباكه بكل الأصناف. **لا تقترح ربطها بجدول `products` تلقائياً إلا إذا طُلب صراحةً.** |
| تسجيل التسليم/الإرجاع على مستوى `case_number` كله (لا لكل صف/منتج على حدة) رغم أن حالة واحدة قد تحوي عدة منتجات (BR-010 في `mamelon-erp`) | ✅ **سلوك مقبول ومقصود** | المستخدم صرّح: لا يهمّه تتبّع كل منتج على حدة في `deliveries` — يكفي أن الحالة كاملة تُعلَّم `delivered` مهما كان عدد صفوفها، لأن المندوب يسلّم الصندوق كاملاً في زيارة واحدة (متوافق مع BR-009 في توثيق `mamelon-erp`: الباركود يشفّر `case_number` والمندوب يسلّم كل التركيبات معاً). **لا تقترح فصل التسجيل لكل منتج إلا إذا طُلب صراحةً.** |
| تغيير الحالة لـ«قيد التوصيل» (`out_for_delivery`) عند إضافة الحالة للسلة | ✅ **مُنفَّذ بالفعل** | تحقَّق من الكود مباشرة (`confirmPickup()`، السطر ~1009) — يحدث فوراً عند الضغط على «📦 خد المحدد للسلة». **لا حاجة لأي عمل هنا، الفكرة موجودة فعلياً.** |

---

## 3. بنود مفتوحة (لم يُتخذ قرار بعد — بانتظار توجيه المستخدم)

### 🔴 توكن بوت تليجرام مكشوف في الكود المصدري (أولوية عالية)
```js
const TELEGRAM_BOT_TOKEN = "8721178342:AAEcfnw2rFvEoVCee_kkdONPEjMRT2fAnnU";
const TELEGRAM_CHAT_ID = "-5306056456";
```
هذا **ليس كمفتاح Supabase الـ anon** (ذلك مصمَّم أصلاً ليكون علنياً وتحميه سياسات RLS). توكن بوت تليجرام **سرّ حقيقي** — أي شخص يفتح "عرض المصدر" (`view-source:`) في متصفحه يحصل عليه، ويستطيع:
- انتحال شخصية البوت وإرسال رسائل باسمه لنفس المجموعة (تضليل، رسائل مزيّفة تبدو رسمية).
- إن كان للبوت صلاحيات إضافية (قراءة رسائل، إدارة مجموعة)، استغلالها بالكامل.

**التوصية:** توليد توكن جديد من BotFather في تليجرام (`/revoke` أو `/newtoken`) بغض النظر عن الحل المعتمد لاحقاً، لأن التوكن الحالي مكشوف علناً بمجرد نشر هذا الملف كصفحة ويب.

**خيارات المعالجة الممكنة (لم يُختر أي منها بعد):**
1. **الأبسط:** قبول المخاطرة كما هي (تطبيق داخلي، شبكة محدودة الاستخدام) — فقط تدوير التوكن دورياً.
2. **الأصح تقنياً:** نقل استدعاء تليجرام لدالة خادم (Supabase Edge Function) بحيث لا يظهر التوكن في كود العميل إطلاقاً — يحتاج بنية إضافية (Edge Function واحدة بسيطة).

### 🟡 ثغرة XSS مخزَّنة — لا تهريب عند العرض
كل استخدامات `el.innerHTML = \`...${متغيّر}...\`` (المواضع الرئيسية: دالة `renderSearchedCases()`، ودالة عرض السلة داخل `loadCart()`، ودالة `loadReturned()`) تحقن نصوصاً قادمة من القاعدة (`clinic_name`, `patient_name`, `case_number`, `doctor_name`, `reason`) مباشرة كـ HTML بلا أي تنظيف.

**الخطر عملياً:** لو موظف استقبال في `mamelon-erp` كتب اسم عيادة يحوي وسم HTML/JS (بالخطأ أو بقصد)، سيُنفَّذ ذلك الكود فعلياً في متصفح المندوب لحظة عرض تلك الحالة. احتمال حدوثه ضعيف (يتطلب مصدراً داخلياً)، لكنه ثغرة حقيقية تقنياً.

**الحل التقني القياسي (بسيط، لا يغيّر أي سلوك ظاهري):** دالة `escapeHtml()` صغيرة تُمرَّر عليها كل القيم النصية القادمة من القاعدة قبل إدراجها في القوالب:
```js
function escapeHtml(str) {
  const div = document.createElement('div');
  div.textContent = str ?? '';
  return div.innerHTML;
}
```
ثم استبدال `${c.clinic_name}` بـ `${escapeHtml(c.clinic_name)}` في كل مكان يُدرَج فيه نص من القاعدة. **لم يُنفَّذ بعد — بانتظار قرار المستخدم إن كان يريد المعالجة الآن أو لاحقاً.**

### 🟢 غياب تام لدعم العمل أوفلاين (معلومة للسياق فقط، لا يتطلب فعلاً بالضرورة)
لا يوجد أي طابور IndexedDB ولا Service Worker — عكس `mamelon-erp` الذي بُني له نظام أوفلاين كامل (ADR-010/011) بسبب انقطاعات الإنترنت الموثّقة في المعمل (2–5 ساعات أحياناً). المندوب في الميدان قد يواجه ظروف اتصال مشابهة أو أسوأ. **هذا ليس عيباً بالضرورة** — قد يكون مقبولاً لأن المسح بالكاميرا والتحقق الفوري من "من يحمل الحالة حالياً" (`current_holder=is.null`) يحتاجان اتصالاً حياً أصلاً لتفادي تعارض مندوبَين على نفس الحالة. **لم يُطلب أي عمل هنا — للعلم فقط.**

---

## 4. قواعد صارمة لأي جلسة قادمة تعمل على هذا الملف

1. **هذا المشروع منفصل تماماً عن `mamelon-erp`** رغم مشاركة نفس قاعدة بيانات Supabase. لا تدمج كوداً بين المشروعين، ولا تنسخ ملفات، ولا تفترض بنية React/Vite هنا — هذا HTML خام بجافاسكربت عادي.
2. **لا تقترح إعادة هيكلة كبرى** (تحويله لـ React، تقسيمه لملفات متعددة) ما لم يُطلب صراحة — طبيعته كملف واحد بسيط مقصودة لسهولة النشر على هاتف المندوب.
3. **راجع قسم "القرارات المتخذة" أعلاه قبل اقتراح أي إصلاح** — ثلاثة بنود بدت كأخطاء في المراجعة الأولى تبيّن أنها سلوك مقصود بعد نقاش مباشر مع المستخدم. لا تُعِد اقتراحها كأخطاء.
4. **أي تعديل في الكود:** افتح الملف، اعرض التغيير المقترح، واحصل على موافقة صريحة قبل التطبيق — لم يُطلب أي تنفيذ تلقائي حتى الآن، فقط مراجعة وتوثيق.
